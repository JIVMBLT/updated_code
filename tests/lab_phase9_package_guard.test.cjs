#!/usr/bin/env node
'use strict';const fs=require('fs'),path=require('path'),assert=require('assert');const R=path.resolve(__dirname,'..');function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);}const files=walk(R);assert(!files.some(f=>/\.(patch|diff)$/i.test(f)));assert(!files.some(f=>/APPLY_FASE.*\.ps1$/i.test(f)));const cfg=fs.readFileSync(path.join(R,'core/config.js'),'utf8');assert(cfg.includes('lab-phase9-bootstrap.js'));const auth=fs.readFileSync(path.join(R,'lab/runtime/lab-auth.js'),'utf8');assert(auth.includes('ManttoLabPhase9Ready'));console.log('FASE 9 package guard: OK');
